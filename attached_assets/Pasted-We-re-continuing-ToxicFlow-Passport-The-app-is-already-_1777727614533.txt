We’re continuing ToxicFlow Passport.

The app is already built and running. Now the next task is to deploy the contracts for real and make the live swap path use Sepolia ETH ↔ real Sepolia USDC, not any project test token or fake/mock token.

Do not rebuild the app from scratch.

Do not redesign the UI unless a small change is required to support the real deployment.

Focus on deployment, configuration, and replacing the current test-token swap path with real Sepolia USDC.

==================================================
CURRENT STATE
==================================================

The dApp already has:

Frontend:
- Landing
- Dashboard
- Passport
- Swap
- Records
- Pool Defense
- Developers
- Demo

API routes:
- /api/diagnostics
- /api/resolver
- /api/quote
- /api/events
- /api/evidence/anchor
- /api/evidence/anchored
- /api/demo/set-tier
- /api/keeperhub/trigger

Contracts:
- PassportRegistry.sol
- ToxicFlowHook.sol
- Foundry deployment scripts
- Pool initialization scripts
- Tier seeding scripts
- Unit tests

Current remaining task:
Deploy everything properly and make the real swap pair:

Sepolia ETH → real Sepolia USDC

NOT:
- tfUSDC
- mock USDC
- mock ERC20
- internal test token
- fake USDC

==================================================
PRIMARY GOAL
==================================================

Deploy the ToxicFlow contracts and initialize a real Uniswap v4 hooked pool using:

Token in:
- Sepolia ETH

Token out:
- real Sepolia USDC

Real Sepolia USDC address:
0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238

After deployment, the user should be able to:

1. connect wallet
2. register passport
3. choose or resolve passport tier
4. quote ETH → Sepolia USDC
5. approve if required
6. swap ETH → Sepolia USDC
7. have ToxicFlowHook apply tier-based fee
8. emit SwapFeeApplied
9. auto-anchor record to 0G
10. update Records and Pool Defense

==================================================
IMPORTANT RULES
==================================================

Do NOT use fake tokens for the main swap path.

Do NOT show “Mock ERC20” anywhere in user-facing UI.

Do NOT label tfUSDC as real USDC.

If a fallback test token remains, it must be hidden from the main flow or clearly labeled as a fallback/dev-only test token.

The public Swap page and Demo page should prioritize:

ETH ↔ Sepolia USDC

==================================================
ENVIRONMENT CHECK
==================================================

Before deploying, inspect the existing environment variables and repo config.

Check for:

- SEPOLIA_RPC_URL
- NEXT_PUBLIC_RPC_URL or VITE_RPC_URL
- DEPLOYER_PRIVATE_KEY
- SCORER_PRIVATE_KEY
- ZG_SIGNER_PRIVATE_KEY
- ZG_RPC_URL
- KEEPERHUB_API_URL
- KEEPERHUB_API_KEY
- KEEPERHUB_WORKFLOW_ID
- DEMO_MODE
- NEXT_PUBLIC_SELF_REGISTER_DEPLOYED or VITE_SELF_REGISTER_DEPLOYED

Important:
The previous report mentioned VITE_* variables, but the existing project may use NEXT_PUBLIC_* if it is Next.js.

Do not assume one naming convention blindly.

Inspect the project and use the correct env system.

If the app is Vite, use VITE_*.
If the app is Next.js, use NEXT_PUBLIC_* for client-visible variables and normal env vars for server-only secrets.

Do not expose private keys to the client.

==================================================
DEPLOYMENT TASKS
==================================================

1. Verify Foundry
Run:

forge --version

If Foundry is missing, install it or report the exact installation steps:

curl -L https://foundry.paradigm.xyz | bash
foundryup

2. Verify deployer balances
Check deployer address from DEPLOYER_PRIVATE_KEY.

Verify:
- Sepolia ETH balance
- Sepolia USDC balance

USDC address:
0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238

If deployer has no Sepolia USDC:
- report exact deployer address
- tell user to fund it from Circle faucet:
  https://faucet.circle.com
- recommend at least 100 Sepolia USDC for pool liquidity/testing
- do not fake pool creation

3. Deploy PassportRegistry
Use the existing Deploy.s.sol or equivalent script.

Deploy:
- PassportRegistry

Confirm:
- selfRegister exists
- getTier works
- hasPassport works
- setTier works for admin

4. Deploy ToxicFlowHook
Deploy the hook connected to:
- Uniswap v4 Sepolia PoolManager
- deployed PassportRegistry or TierResolver

Confirm:
- hook address has the correct low-bit flags for beforeSwap
- getHookPermissions returns expected beforeSwap permission
- previewFee works

5. Initialize Uniswap v4 hooked pool
Create a real ToxicFlow pool using:

- ETH / Sepolia USDC
- USDC address: 0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238
- ToxicFlowHook attached from pool initialization

Important:
Do not try to attach ToxicFlowHook to an existing normal pool if Uniswap v4 PoolKey makes hooks immutable.

If hooks are immutable at pool creation, create a new hooked ETH/USDC pool.

6. Add liquidity
Add enough liquidity to support realistic demo/test swaps.

Target:
- enough for swaps like 0.001 ETH, 0.005 ETH, 0.01 ETH if deployer funds allow

If liquidity is insufficient:
- report the exact amount of ETH and USDC needed
- do not make the UI pretend large swaps are supported

7. Update deployment config
Update whichever files the repo uses:

Possible files:
- contracts/deployment.json
- src/config/contracts.ts
- lib/config/contracts.ts
- .env.local
- .env.example

Set:
- PASSPORT_REGISTRY address
- TOXIC_FLOW_HOOK address
- POOL_MANAGER address
- POOL_SWAP_TEST or router address
- V4_QUOTER address
- POOL_ID
- USDC address
- chain ID 11155111

Use correct prefixes:
- NEXT_PUBLIC_* for Next.js
- VITE_* for Vite

8. Update frontend token config
Make Swap page use:

Input:
- Sepolia ETH

Output:
- Sepolia USDC

Token display:
- ETH icon/logo
- USDC icon/logo
- symbol: USDC
- decimals: 6
- address: 0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238

Remove or hide:
- Mock ERC20
- tfUSDC from main swap path
- any fake/test token from the primary user flow

If tfUSDC remains, place it only in developer fallback or diagnostics.

9. Update Demo page
Demo should also use ETH → real Sepolia USDC.

Demo flow:
- choose Neutral / Trusted / Toxic
- set tier on connected wallet through real route
- quote ETH → USDC
- show expected ToxicFlow fee:
  - Neutral: 30 bps
  - Trusted: 10 bps
  - Toxic: 80 bps
- execute real swap
- show actual fee from SwapFeeApplied event
- auto-store record
- show LP/AMM protection section

10. Update quote route
Ensure /api/quote supports:
- decimal input such as 0.001
- ETH input decimals 18
- USDC output decimals 6
- real v4 quoter
- clean liquidity errors

Return:
- inputAmountHuman
- inputAmountBaseUnits
- outputAmountHuman
- outputAmountBaseUnits
- feeBps
- tier
- pair
- mode

11. Update swap execution
Ensure the real browser swap path sends transactions against the new ETH/USDC hooked pool.

After swap:
- fetch SwapFeeApplied event
- auto-anchor record to 0G
- update Records page
- update Pool Defense monitoring

12. Keep KeeperHub live
Do not remove KeeperHub.

Verify:
- /api/keeperhub/trigger works
- Pool Defense can still trigger workflow
- payload includes latest record/evidence

13. Keep 0G live
Verify:
- auto-anchoring works after real ETH/USDC swap
- storageRef is returned
- Records page shows stored status

==================================================
TESTING REQUIREMENTS
==================================================

Run:

- pnpm tsc --noEmit or corepack pnpm tsc --noEmit
- pnpm build or corepack pnpm build
- forge build
- forge test

After deployment, test:

1. Diagnostics
GET /api/diagnostics

2. Resolver
GET /api/resolver?trader=<DEPLOYER_OR_TEST_WALLET>&method=all

3. Quote decimal
GET /api/quote?trader=<WALLET>&amountIn=0.001

Expected:
- no BigInt error
- pair is ETH/USDC
- feeBps returned
- output in USDC decimals

4. Demo set tier
POST /api/demo/set-tier

Test:
- tier 0
- tier 1
- tier 2

5. Real swap
Execute one tiny ETH → USDC swap through UI or script.

6. Event check
GET /api/events?txHash=<SWAP_TX_HASH>

Confirm:
- SwapFeeApplied event exists
- fee matches tier

7. 0G anchor
Confirm record auto-stores after swap.

8. Records
Confirm record appears in /records.

9. Pool Defense
Confirm Pool Defense reads recent records and KeeperHub trigger still works.

==================================================
UI REQUIREMENTS
==================================================

Update all user-facing labels:

Use:
- ETH
- USDC
- Sepolia USDC
- ToxicFlow swap fee
- Network gas fee

Do NOT show:
- Mock ERC20
- tfUSDC in main flow
- fake USDC
- internal token names
- PoolSwapTest
- raw backend route names

If fallback pool is still used, show:

“ETH / ToxicFlow Test USDC fallback pool”

But only if real ETH/USDC pool cannot be deployed.

==================================================
EXPECTED FINAL REPORT
==================================================

Report:

1. Foundry status
2. Deployer address
3. Sepolia ETH balance
4. Sepolia USDC balance
5. Contracts deployed
6. Contract addresses
7. Hook address
8. Pool ID
9. Whether ETH/USDC hooked pool is live
10. Amount of liquidity added
11. Whether frontend now uses ETH/USDC
12. Whether demo now uses ETH/USDC
13. Whether tfUSDC/mock token was removed from main flow
14. Quote test result
15. Swap test result
16. Swap tx hash if successful
17. Event test result
18. 0G anchor result
19. KeeperHub trigger result
20. Files changed
21. Commands run
22. Remaining blockers
23. Exact user action required if anything remains blocked

==================================================
SUCCESS CRITERIA
==================================================

This pass is complete only if:

- contracts are deployed
- ETH/real-Sepolia-USDC hooked pool is initialized if possible
- liquidity is added
- frontend uses ETH/USDC as main pair
- demo uses ETH/USDC as main pair
- swap emits real fee event
- fee changes by passport tier
- record is auto-stored
- Pool Defense still works
- KeeperHub still triggers
- no fake/mock token is presented as main user flow
- build/tests pass

If ETH/USDC deployment is blocked, stop and report exact blocker instead of pretending the fallback is complete.