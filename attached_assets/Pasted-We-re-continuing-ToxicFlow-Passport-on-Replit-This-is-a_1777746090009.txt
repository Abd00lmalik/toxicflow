We’re continuing ToxicFlow Passport on Replit.

This is a production deployment cleanup and Vercel build-fix pass.

Do not rebuild the product from scratch.

Fix the current Replit/GitHub/Vercel deployment issue, clean the repo, remove irrelevant files, rename AI-looking files to professional names, and push the corrected production-ready project to GitHub.

==================================================
CURRENT DEPLOYMENT FAILURE
==================================================

Vercel build failed from:

github.com/Abd00lmalik/ToxicFlow

Branch:
main

Commit:
918c819

Vercel detected a pnpm workspace and built:

artifacts/api-server

The failed package:

@workspace/api-server@0.0.0

The error:

src/app.ts(10,3): error TS2349:
This expression is not callable.
Type 'typeof import("pino-http/index")' has no call signatures.

src/app.ts(13,11): error TS7006:
Parameter 'req' implicitly has an 'any' type.

src/app.ts(20,11): error TS7006:
Parameter 'res' implicitly has an 'any' type.

This strongly suggests either:

1. Vercel is building the wrong package, or
2. artifacts/api-server is unnecessary/stale, or
3. api-server is needed but has TypeScript/import issues.

You must inspect and decide. Do not guess.

==================================================
PRIMARY OBJECTIVE
==================================================

Make the GitHub repo production-deployable on Vercel.

The final repo should contain only files relevant to:

- ToxicFlow frontend
- ToxicFlow API routes
- ToxicFlow contracts
- ToxicFlow deployment scripts
- ToxicFlow config
- ToxicFlow docs
- ToxicFlow tests
- production deployment

Remove irrelevant generated/scaffold/artifact files.

Fix Vercel so it builds the actual ToxicFlow app, not a stale or accidental artifact package.

==================================================
REPLIT-SPECIFIC REQUIREMENTS
==================================================

This is running inside Replit.

Use the Replit environment carefully.

Before changing files:

1. Inspect repo root.
2. Inspect git status.
3. Inspect package structure.
4. Inspect whether this is a monorepo or accidental workspace.
5. Inspect what Vercel will build.
6. Inspect whether `artifacts/api-server` is needed.
7. Do not assume the current root is clean.
8. Do not delete secrets or env config from Replit Secrets.
9. Do not print secrets.

If GitHub push is needed, use the Replit Secret:

GITHUB_TOKEN

Rules:
- Do not print the token.
- Do not write it to files.
- Do not commit it.
- Use it only for git push authentication if needed.

If authentication is already configured, use normal git push.

==================================================
TASK 1: INSPECT PROJECT STRUCTURE
==================================================

Inspect:

- package.json
- pnpm-workspace.yaml
- pnpm-lock.yaml
- turbo.json if present
- vercel.json if present
- next.config.*
- vite.config.*
- tsconfig.json
- apps/
- app/
- src/
- artifacts/
- contracts/
- lib/
- components/
- public/
- docs/
- scripts/
- README files
- GitHub remote

Determine:

1. Is the production ToxicFlow app Next.js or Vite?
2. Where is the actual production app located?
3. Why did Vercel build `artifacts/api-server`?
4. Is `artifacts/api-server` required?
5. Is `artifacts/api-server` stale/generated/unrelated?
6. Is the pnpm workspace necessary?
7. Should the workspace be simplified?
8. Should Vercel root/build config be changed?
9. What files are required for deployment?

Produce an internal cleanup plan before deleting anything.

==================================================
TASK 2: FIX VERCEL BUILD TARGET
==================================================

If the ToxicFlow app is a Next.js app at repo root:

- ensure root package.json has correct build script
- ensure Vercel builds the root app
- add/update vercel.json if needed
- remove workspace package references that cause Vercel to build the wrong package

If ToxicFlow app is in a subdirectory:

- configure vercel.json or repo structure so Vercel builds the correct subdirectory
- do not let Vercel default to artifacts/api-server

If `artifacts/api-server` is not required:

- remove it from pnpm-workspace.yaml
- delete it if safe
- remove any scripts pointing to it
- update lockfile if needed
- ensure app still builds

If `artifacts/api-server` is required:

- fix its TypeScript errors
- ensure it is not the only Vercel target unless intended
- make sure it does not block frontend deployment unnecessarily

Final result:
Vercel should build the actual ToxicFlow production app.

==================================================
TASK 3: FIX API-SERVER TYPESCRIPT ERRORS ONLY IF NEEDED
==================================================

If `artifacts/api-server` must remain, fix:

1. `pino-http` import issue
2. implicit `any` for req/res

Inspect package types before choosing import style.

Possible valid fixes:
- `import pinoHttp from "pino-http"` with correct tsconfig/esModuleInterop
- `import pinoHttp = require("pino-http")`
- or the correct named import if package types support it

Also type Express handlers:

```ts
import type { Request, Response } from "express"

Then annotate req/res or use inferred Express types correctly.

Run targeted build:

pnpm --filter @workspace/api-server build

But if this package is not needed, delete/remove it instead of maintaining dead code.

================================================== TASK 4: DELETE IRRELEVANT FILES

Delete any file/folder not relevant to production deployment.

Inspect before deleting.

Likely candidates:

stale artifacts

generated scratch folders

unused api-server

duplicate app versions

old Replit experiments

old prompt files

pasted markdown files

temporary files

output reports

build artifacts committed by mistake

unused mock scaffolds

unused sample packages

dead workspace packages

AI generated scratch docs

non-production notes


Bad file name patterns to search:

claude

codex

replit prompt

gemini

ai

prompt

pasted

scratch

temp

old

backup

final-final

generated

artifact

sample

placeholder

test-output

report

response


Do not delete:

actual app source

actual API routes

required contract artifacts/ABIs

deployment addresses/config

contract source

deployment scripts

production docs

tests

package lockfile

public assets


================================================== TASK 5: RENAME AI-LOOKING FILES TO PROFESSIONAL NAMES

Any file name that makes the project look AI-generated should be renamed, moved, or deleted.

Examples of bad names:

claude-prompt.md

replit-prompt.md

codex-output.md

gemini-analysis.md

ai-plan.md

pasted-markdown.md

generated-ui.tsx

scratch.tsx

temp.ts

prompt.txt

report-from-ai.md


If useful, rename to:

ARCHITECTURE.md

DEPLOYMENT.md

INTEGRATION_GUIDE.md

PROTOCOL_OVERVIEW.md

DESIGN_SYSTEM.md

TESTING.md

TROUBLESHOOTING.md

RELEASE_NOTES.md


If not useful, delete.

No committed production file should reveal:

Claude

Codex

Replit Agent

Gemini

prompt history

AI-generated wording

pasted chat logs


Update imports or links if renaming affects code.

================================================== TASK 6: CLEAN PACKAGE AND WORKSPACE CONFIG

After deleting/renaming:

update package.json scripts

update pnpm-workspace.yaml

update tsconfig paths if needed

update imports if needed

update vercel.json if needed

update README if needed

reinstall if lockfile needs refresh


The final package/workspace structure must be simple and production-safe.

Do not leave references to deleted packages.

================================================== TASK 7: SECRET SAFETY

Before committing or pushing, verify:

.env.local is ignored

Replit secrets are not written to repo

DEPLOYER_PRIVATE_KEY is not committed

SCORER_PRIVATE_KEY is not committed

ZG_SIGNER_PRIVATE_KEY is not committed

KEEPERHUB_API_KEY is not committed

GITHUB_TOKEN is not committed

no raw private keys are committed

no workflow API keys are committed


Search for:

PRIVATE_KEY

DEPLOYER_PRIVATE_KEY

SCORER_PRIVATE_KEY

ZG_SIGNER_PRIVATE_KEY

KEEPERHUB_API_KEY

GITHUB_TOKEN

wfb_

kh_

64-character hex private-key-like strings


It is okay for .env.example to contain variable names with empty values. It is not okay to contain real secrets.

Do not print secret values in logs or reports.

================================================== TASK 8: VERIFY TOXICFLOW FUNCTIONALITY STILL EXISTS

After cleanup, verify these routes/pages still exist and build:

Pages:

Landing

Dashboard

Passport

Swap

Records

Pool Defense

Developers

Demo


API routes:

/api/diagnostics

/api/resolver

/api/quote

/api/events

/api/evidence/anchor

/api/evidence/anchored

/api/demo/set-tier

/api/keeperhub/trigger


Contracts/config:

PassportRegistry

ToxicFlowHook

deployment config

ABI/config files

Uniswap v4 config

0G config

KeeperHub config

Sepolia USDC config


Do not remove required production functionality.

================================================== TASK 9: LOCAL BUILD VERIFICATION

Run the correct commands based on the final repo structure.

At minimum:

pnpm install
pnpm build

Also run if available:

pnpm tsc --noEmit
pnpm lint
forge build
forge test

If monorepo remains, run:

pnpm -r build

or targeted frontend build:

pnpm --filter <frontend-package> build

Only use commands that apply after inspection.

The final build must pass locally.

================================================== TASK 10: VERCEL CONFIG VERIFICATION

Ensure Vercel will build the right app.

If using Next.js root app, create/update vercel.json as needed:

{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm build"
}

Only add this if correct.

If using Vite, configure appropriately.

If using monorepo, configure the correct root/build command.

The final report must state the exact Vercel project settings the user should verify:

Root Directory

Install Command

Build Command

Output Directory if applicable

Environment variables required


================================================== TASK 11: COMMIT AND PUSH TO GITHUB

Push to:

https://github.com/Abd00lmalik/toxicflow

There are already files there.

You may edit, delete, or replace existing files if needed.

Commit in small logical chunks.

Suggested commits:

1. chore: clean production workspace


2. fix: correct vercel build configuration


3. fix: resolve api server build errors only if api-server is retained


4. chore: remove stale generated files


5. docs: organize deployment documentation


6. chore: verify production build



Before each commit:

git status

review staged files

do not stage .env.local

do not stage secrets


Use GITHUB_TOKEN from Replit Secrets if push auth is needed.

Safe push approach if token is required:

use token only in remote URL temporarily

do not print token

reset remote URL back to clean GitHub URL after push


Do not expose token in logs.

================================================== EXPECTED FINAL REPORT

Report:

1. Repo structure found


2. Actual production app location


3. Why Vercel built artifacts/api-server


4. Whether artifacts/api-server was kept or removed


5. If kept, exact TypeScript fix


6. Files deleted


7. Files renamed


8. AI-looking files removed or renamed


9. Workspace/package config changes


10. Vercel config changes


11. Local build command used


12. Build result


13. Typecheck result


14. Secret scan result


15. Git commits made


16. Git push status


17. Final Vercel settings to verify


18. Remaining blockers, if any



================================================== SUCCESS CRITERIA

This pass succeeds only if:

Vercel no longer builds the wrong stale package

production build passes locally

irrelevant deployment files are removed

AI-looking/prompt/scratch files are deleted or renamed professionally

workspace/package config is clean

no secrets are committed

ToxicFlow functionality remains intact

GitHub repo is updated

final deployment instructions are clear